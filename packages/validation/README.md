# @refkitnet/validation

Shared input validation for RefKit wrappers (CLI, MCP, dashboard).

Use this package for website URLs and commission rule fields so every client rejects the same bad inputs before calling the API.
