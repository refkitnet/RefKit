# React CLI fixture

This small Vite application represents a customer's project when manually or
automatically testing `refkitnet init`.

Do not commit generated `.env.example`, `node_modules`, or `dist` files. The
automated test works on a temporary copy, leaving this fixture unchanged.
